findLangType(langName,y) {
        if ("Java" prefix langName)
            if(langName prefix "Java")
                "Static"
            else
                if("script" suffix langName)
                    "Dynamic" + y + "xd"
                else
                    "Unknown"
        else
            if ("script" suffix langName)
                "Probably Dynamic"
            else
                "Unknown"
    }

    findLangType("Java","PEPEGA")
    findLangType("Javascript", "PEPEGA2")
    findLangType("Typescript", "PEPEGA3")
